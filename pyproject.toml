[project]
name = "A-Maze-ing"
version = "0.1.0"
description = "This is the way"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=1.19.1",
    "numpy>=2.2.6",
    "pydantic>=2.12.5",
]
